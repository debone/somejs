'use strict';

var some = require( './some.core' );

// '#'+Math.floor(Math.random()*16777215).toString(16);

var color = function ( world, color ) {
  
};

some.color = color;

module.exports = some;